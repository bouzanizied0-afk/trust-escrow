<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>ستوشي | بروتوكول الانطلاق</title>
    <style>
        body { 
            background: #0a0a0a; 
            color: #ff0000; 
            text-align: center; 
            padding-top: 15%; 
            font-family: 'Courier New', Courier, monospace; 
            overflow: hidden;
        }
        #timer { 
            font-size: 120px; 
            font-weight: bold; 
            text-shadow: 0 0 20px #ff0000; 
        }
        .status-text { 
            font-size: 1.2rem; 
            letter-spacing: 2px; 
            color: #555;
        }
    </style>
</head>
<body>

    <div class="status-text">جاري تحويل المسار إلى الكيان ب...</div>
    <div id="timer">5</div>
    <div class="status-text">بروتوكول TRUST-ESCROW نشط</div>

    <script>
        // الرابط الذي قدمته أنت كوجهة نهائية
        const DESTINATION = "https://bouzanizied0-afk.github.io/trust-escrow/";

        let seconds = 5;
        const countdown = setInterval(() => {
            seconds--;
            document.getElementById('timer').textContent = seconds;
            
            if (seconds <= 0) {
                clearInterval(countdown);
                // تنفيذ القفزة وتطهير السجل لعدم العودة
                window.location.replace(DESTINATION);
            }
        }, 1000);
    </script>
</body>
</html>
